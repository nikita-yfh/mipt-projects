; запихиваем параметры
push 1
pop rax
push 1
pop rbx

; сколько чисел надо
push 30
pop rcx


;call :fib


:fib

; проверяем, надо ли заканчивать это все
push rcx
push rcx
je :end
push 1
sub
pop rcx

; запоминаем предыдущее в стеке
push rbx
; считаем следующее
push rax
push rbx
add
; восстанавливаем из стека
pop rbx
pop rax

push rax
out

; рекурсия наше все
call :fib

ret

:end
hlt

